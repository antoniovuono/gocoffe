import React from 'react';

import * as Styled from './styles';

const Favorites: React.FC = () => {
    return (
        <Styled.Container>
            <Styled.Title>Welcome ASV Studio (Favorites)</Styled.Title>
        </Styled.Container>
    );
};

export default Favorites;
