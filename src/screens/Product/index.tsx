import React from 'react';

import * as Styled from './styles';

const Product: React.FC = () => {
    return (
        <Styled.Container>
            <Styled.Title>Welcome to PDP</Styled.Title>
        </Styled.Container>
    );
};

export default Product;
