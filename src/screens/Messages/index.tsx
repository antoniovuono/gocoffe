import React from 'react';

import * as Styled from './styles';

const Messages: React.FC = () => {
    return (
        <Styled.Container>
            <Styled.Title>Welcome ASV Studio (MESSAGES CART)</Styled.Title>
        </Styled.Container>
    );
};

export default Messages;
